# Seeded design canvas — ~2 MB of vendored editor code, regenerated by
# seed-canvas.mjs from the .dc.html sources in design/
design/portfolio-canvas.html

node_modules/
.DS_Store
Thumbs.db

# Stray npm lockfile from a one-off npx invocation; this is not an npm project
package-lock.json
package.json
